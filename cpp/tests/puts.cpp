/*
 * #include <cstdio>

int main() {
	int rc{};

	 rc = std::puts("Hello World");
	 if (rc == EOF)
		 std::perror("puts()"); // POSIX requires that errno is set

	 rc = std::fputs("Hello World", stdout);
	 if (rc == EOF)
		 std::perror("puts()"); // POSIX requires that errno is set

}
*/
